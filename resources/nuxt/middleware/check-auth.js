
export default ({ store, route, app }) => {

    // Redirect to login if token is not provided.
    /*if ((route.path === '/login' || route.path === '/register' || route.path === '/') === false ) {
        localStorage.getItem('token') === null || undefined ? app.router.push('/login') : ''
    }*/

}